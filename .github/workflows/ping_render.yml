name: Keep Render Awake

on:
  schedule:
    # roda a cada 10 minutos (ajuste se quiser)
    - cron: '*/10 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render app
        run: |
          curl -fsSL https://planta-cliniprev2.onrender.com/api/health || exit 1
